services:
  - type: web
    name: aizen-whatsapp-bot
    env: node
    plan: free
    buildCommand: npm install --legacy-peer-deps
    startCommand: node index.js
    envVars:
      - key: NODE_ENV
        value: production